const domainName = "https://dashboard-backend-psi.vercel.app/api";

export const urls = {
    getImage: `${domainName}/get-images`,
    getText: `${domainName}/get-text`,
    subscribeEmail: `${domainName}/subscribe`,
}